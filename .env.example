# Optional server-side fallbacks. The prototype also accepts session-only keys in the UI.
GLM_API_KEY=
GLM_MODEL=glm-5.2
GLM_BASE_URL=https://open.bigmodel.cn/api/paas/v4
ALPHA_VANTAGE_API_KEY=
# Recommended for SEC fair-access identification in deployed environments.
SEC_USER_AGENT=ClarityInvest/1.0 your-email@example.com
