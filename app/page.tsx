"use client";

import { FormEvent, ReactNode, useEffect, useMemo, useRef, useState } from "react";

type Stage = "questionnaire" | "building" | "dashboard";
type Tab = "overview" | "company" | "agents" | "events" | "risk" | "evidence";
type CompanyTicker = "AAPL" | "MSFT" | "NVDA" | "AMZN";
type Message = { role: "user" | "assistant"; text: string };
type Holding = { ticker: string; name: string; weight: number; theme: string; signal: string; color: string };
type InvestorProfile = Record<string, string | string[]>;
type Quote = { symbol: string; price: number; change: number; changePercent: string; latestTradingDay: string; volume: number; isRealtime?: boolean };
type LiveMarket = {
  fetchedAt: string;
  servedAt: string;
  dataStatus: "fresh" | "cached" | "stale";
  warning?: string;
  dataMode: "public" | "alpha";
  newsTimeBasis: "published" | "updated" | "filed" | "mixed";
  usage: { providerRequests: number; cacheWindowMinutes: number; alphaRequests?: number };
  freshness: { quotesFetchedAt: string; overviewFetchedAt: string; newsFetchedAt: string };
  sources: { quotes: string; fundamentals: string; news: string };
  capabilities: { quotes: boolean; fundamentals: boolean; news: boolean; sentiment: boolean };
  symbols: string[];
  quotes: Quote[];
  overview: {
    symbol: string; name: string; description: string; sector: string; industry: string; marketCapitalization: number;
    peRatio: number; pegRatio: number; eps: number; profitMargin: number; returnOnAssets: number; returnOnEquity: number;
    revenueGrowth: number; earningsGrowth: number; analystTargetPrice: number; fiftyTwoWeekHigh: number; fiftyTwoWeekLow: number;
  };
  news: { title: string; summary: string; source: string; url: string; publishedAt: string; sentiment: string; sentimentScore: number | null; symbols: string[]; timeBasis?: "published" | "updated" | "filed" }[];
  provider: string;
};
type LiveAnalysis = {
  summary: string;
  confidence: number;
  decision: { label: string; rationale: string; reviewTrigger: string };
  agents: { name: string; score: number; verdict: string; findings: string[] }[];
  bullCase: string[];
  bearCase: string[];
  riskFlags: string[];
  learningNotes: string[];
};
type ApiStatus = "demo" | "loading" | "live" | "error";

const initialHoldings: Holding[] = [
  { ticker: "AAPL", name: "Apple", weight: 35, theme: "Quality technology", signal: "Core", color: "#143f36" },
  { ticker: "MSFT", name: "Microsoft", weight: 30, theme: "Cloud & AI", signal: "Overweight", color: "#4b8c77" },
  { ticker: "NVDA", name: "NVIDIA", weight: 20, theme: "AI infrastructure", signal: "Catalyst watch", color: "#d4a44f" },
  { ticker: "CASH", name: "Cash reserve", weight: 15, theme: "Defensive buffer", signal: "Guardrail", color: "#d6ddd8" },
];

const revisedHoldings: Holding[] = [
  { ticker: "AAPL", name: "Apple", weight: 35, theme: "Quality technology", signal: "Core", color: "#143f36" },
  { ticker: "AMZN", name: "Amazon", weight: 30, theme: "Commerce & cloud", signal: "User directed", color: "#756390" },
  { ticker: "NVDA", name: "NVIDIA", weight: 20, theme: "AI infrastructure", signal: "Catalyst watch", color: "#d4a44f" },
  { ticker: "CASH", name: "Cash reserve", weight: 15, theme: "Defensive buffer", signal: "Guardrail", color: "#d6ddd8" },
];

const glossary = {
  "Max drawdown": ["The largest peak-to-trough fall over a measured period.", "This portfolio shows a −14.2% illustrative drawdown versus −10.8% for the S&P 500. The extra downside comes from concentrated growth exposure."],
  "Sharpe ratio": ["Excess return earned per unit of historical volatility.", "A 1.18 sample Sharpe is stronger than the benchmark, but it is backward-looking and sensitive to the time window."],
  "Beta": ["Sensitivity to broad-market movement; 1.0 roughly matches the market.", "The 1.16 portfolio beta suggests a 1% market move could translate to about a 1.16% portfolio move, before company-specific effects."],
  "Tracking error": ["How differently a portfolio moves from its benchmark.", "The 8.4% estimate is meaningful because this is a focused portfolio, not an S&P 500 substitute."],
  "ROA": ["Return on assets measures how efficiently a company turns its asset base into profit.", "Compare ROA within similar business models. Asset-light software and semiconductor firms can naturally differ from retailers or banks."],
  "P/E ratio": ["Share price divided by earnings per share: the price paid for one dollar of earnings.", "A high P/E may reflect superior growth, but it also increases the cost of disappointment."],
  "Gross margin": ["Revenue left after direct costs, before operating expenses.", "Margin direction helps separate durable pricing power from growth that requires rising costs."],
  "Diversification": ["Combining return drivers so one company or theme does not dominate outcomes.", "Four allocations diversify by name, but the portfolio remains concentrated in US mega-cap growth."],
  "Model confidence": ["A stability score for evidence coverage, profile fit and agent agreement.", "It is not a probability of profit. Current confidence is reduced by valuation and NVIDIA event risk."],
} as const;
type GlossaryTerm = keyof typeof glossary;

const companies: Record<CompanyTicker, {
  name: string; sector: string; price: string; day: string; dayTone: string; cap: string; pe: string; roa: string;
  revenue: string; margin: string; eps: string; thesis: string; risk: string; catalyst: string; verdict: string;
  performance: number[]; benchmark: number[]; health: { label: string; value: number }[]; source: string;
}> = {
  AAPL: {
    name: "Apple Inc.", sector: "Technology · Consumer devices", price: "$316.50", day: "−0.10%", dayTone: "negative", cap: "$4.66T", pe: "36.4×", roa: "24.7%*",
    revenue: "+16% YoY", margin: "50.1%", eps: "$2.02 · +29%", verdict: "QUALITY / WATCH VALUATION",
    thesis: "Installed-base economics, services mix and brand pricing power support durable cash generation.",
    risk: "Premium valuation and a tariff-refund benefit in the latest margin reduce room for execution misses.",
    catalyst: "Next earnings update and services growth disclosure.",
    performance: [100,103,102,106,111,109,116,121,125,128,133,131], benchmark: [100,101,103,104,107,108,110,111,113,114,116,118],
    health: [{label:"Growth",value:78},{label:"Profitability",value:91},{label:"Balance sheet",value:82},{label:"Valuation",value:46},{label:"Momentum",value:73}],
    source: "https://www.apple.com/newsroom/2026/07/apple-reports-third-quarter-results/",
  },
  MSFT: {
    name: "Microsoft Corp.", sector: "Technology · Software & cloud", price: "$481.39", day: "−0.60%", dayTone: "negative", cap: "$3.58T", pe: "28.7×", roa: "20.6%*",
    revenue: "+18% YoY", margin: "46.2%*", eps: "$4.14*", verdict: "OVERWEIGHT",
    thesis: "Cloud revenue, contracted demand and platform breadth create one of the strongest earnings-quality profiles in the portfolio.",
    risk: "AI infrastructure spending, capacity timing and OpenAI-related accounting can complicate margin interpretation.",
    catalyst: "Azure growth, capex productivity and commercial RPO conversion.",
    performance: [100,101,105,108,112,115,114,119,123,126,130,129], benchmark: [100,101,103,104,107,108,110,111,113,114,116,118],
    health: [{label:"Growth",value:84},{label:"Profitability",value:93},{label:"Balance sheet",value:89},{label:"Valuation",value:62},{label:"Momentum",value:76}],
    source: "https://www.microsoft.com/en-us/investor/earnings/fy-2026-q4/press-release-webcast",
  },
  NVDA: {
    name: "NVIDIA Corp.", sector: "Technology · Semiconductors", price: "$218.73", day: "+0.54%", dayTone: "positive", cap: "$5.33T", pe: "33.3×", roa: "68.2%*",
    revenue: "+85% YoY", margin: "74.9%", eps: "$2.39", verdict: "HOLD / EVENT WATCH",
    thesis: "Accelerated-computing demand and exceptional margins keep the fundamental signal strong.",
    risk: "Very high expectations, customer concentration, policy exposure and an imminent earnings catalyst amplify gap risk.",
    catalyst: "The next earnings release, major guidance update or material policy change.",
    performance: [100,108,111,118,115,126,132,138,144,151,147,154], benchmark: [100,101,103,104,107,108,110,111,113,114,116,118],
    health: [{label:"Growth",value:98},{label:"Profitability",value:97},{label:"Balance sheet",value:91},{label:"Valuation",value:55},{label:"Momentum",value:88}],
    source: "https://www.sec.gov/Archives/edgar/data/1045810/000104581026000051/q1fy27pr.htm",
  },
  AMZN: {
    name: "Amazon.com Inc.", sector: "Consumer · Commerce & cloud", price: "$262.29", day: "−1.34%", dayTone: "negative", cap: "$2.86T", pe: "21.1×", roa: "13.2%*",
    revenue: "+17%* YoY", margin: "48.2%*", eps: "$12.43 TTM", verdict: "USER-DIRECTED / REVIEW",
    thesis: "AWS, advertising and improving retail efficiency provide multiple earnings drivers.",
    risk: "Retail capital intensity and the change from MSFT add a different margin and cash-flow profile.",
    catalyst: "AWS growth, retail operating margin and capex guidance.",
    performance: [100,102,99,105,109,112,116,118,121,124,123,127], benchmark: [100,101,103,104,107,108,110,111,113,114,116,118],
    health: [{label:"Growth",value:76},{label:"Profitability",value:72},{label:"Balance sheet",value:76},{label:"Valuation",value:74},{label:"Momentum",value:69}],
    source: "https://ir.aboutamazon.com/",
  },
};

const events = [
  { date:"20 AUG", type:"MARKET", ticker:"S&P 500", severity:"Medium", tone:"mixed", title:"Broad market remains strong but weekly momentum cooled", summary:"The index is up 12.6% year to date after a 1.0% weekly pullback through 19 Aug.", impact:"High-growth holdings remain supported by the trend, but benchmark-relative concentration matters more near record levels.", response:"Keep the 15% cash floor; do not chase a one-day move.", source:"AP market summary", href:"https://apnews.com/article/cb67d83b0638d31e5fe3c80c8de5934c" },
  { date:"19 AUG", type:"MACRO", ticker:"ALL", severity:"High", tone:"mixed", title:"Fed minutes follow a 3.50–3.75% target-range hold", summary:"The July meeting kept policy unchanged. Duration-sensitive growth valuations remain exposed to rate expectations.", impact:"Affects discount rates for all three equities and supports the value of a cash reserve.", response:"Monitor real yields and valuation compression, not headlines alone.", source:"Federal Reserve", href:"https://www.federalreserve.gov/monetarypolicy/fomcminutes20260729.htm" },
  { date:"30 JUL", type:"EARNINGS", ticker:"AAPL", severity:"Medium", tone:"positive", title:"Revenue rose 16%; EPS increased 29%", summary:"Quarterly revenue reached $109.4B and gross margin was 50.1%, including a favorable tariff-refund effect.", impact:"Supports the quality thesis, while the one-off benefit lowers confidence in extrapolating the full margin.", response:"Maintain 35%; require margin-quality follow-up.", source:"Apple Newsroom", href:companies.AAPL.source },
  { date:"29 JUL", type:"EARNINGS", ticker:"MSFT", severity:"Medium", tone:"positive", title:"Microsoft Cloud revenue reached $59.3B", summary:"Cloud revenue increased 27%; commercial remaining performance obligation grew 84% to $678B.", impact:"Strengthens contracted-demand visibility, with capex efficiency still a core debate item.", response:"Maintain 30% overweight within concentration guardrail.", source:"Microsoft IR", href:companies.MSFT.source },
  { date:"PAST EXAMPLE", type:"CATALYST", ticker:"NVDA", severity:"High", tone:"watch", title:"An earnings release can materially change the NVIDIA case", summary:"This historical teaching card shows how growth, margins and guidance can affect a concentrated holding.", impact:"A 20% position can materially move the portfolio if guidance changes expectations.", response:"Cap exposure and define in advance what evidence would lead to holding, reducing or adding.", source:"NVIDIA IR example", href:"https://investor.nvidia.com/" },
];

const questions = [
  { id:"goal", step:"01 · YOUR GOAL", title:"What should this money achieve?", note:"Your goal helps us explain which trade-offs may make sense.", options:["Long-term growth","Build future income","Preserve capital","Learn by investing"] },
  { id:"budget", step:"02 · AMOUNT", title:"How much money should this plan use?", note:"Keep emergency savings and money needed soon outside the plan.", options:[] },
  { id:"horizon", step:"03 · YOUR TIMELINE", title:"When might you need the money?", note:"More time can make it easier to wait through market falls.", options:["Less than 2 years","2–3 years","3–5 years","More than 5 years"] },
  { id:"loss", step:"04 · POSSIBLE LOSS", title:"What temporary loss could you tolerate?", note:"This helps describe your comfort level; it does not put a limit on real losses.", options:["Up to 5%","Up to 10%","Up to 20%","More than 20%"] },
  { id:"reaction", step:"05 · IF MARKETS FALL", title:"The market falls 20%. What do you do?", note:"How you may react under pressure is an important part of the plan.", options:["Sell everything","Reduce exposure","Hold and review","Invest gradually"] },
  { id:"level", step:"06 · EXPLANATION STYLE", title:"How much detail would you like?", note:"This changes how we explain the plan, not the facts or risk limits.", options:["Beginner — explain each term","Intermediate — connect data and ideas","Advanced — focus on assumptions"] },
  { id:"rules", step:"07 · YOUR PREFERENCES", title:"Is there anything you want to avoid or include?", note:"You can change a preference later and compare the old and new examples.", options:["Avoid fossil fuels","Avoid tobacco","Prefer technology","Keep at least 10% cash"] },
];

const tabs: { id: Tab; label: string; number: string }[] = [
  {id:"overview",label:"My plan",number:"01"},{id:"company",label:"My stocks",number:"02"},{id:"events",label:"Recent updates",number:"03"},
  {id:"risk",label:"Risk check",number:"04"},{id:"agents",label:"Why these picks",number:"05"},{id:"evidence",label:"Sources",number:"06"},
];

function Logo() { return <div className="brand"><span>C</span><div><b>ClarityInvest</b><small>Investing, explained clearly</small></div></div>; }
function Learn({ term, explain }: { term: GlossaryTerm; explain: (term: GlossaryTerm) => void }) { return <button className="learn" onClick={() => explain(term)}>{term}<sup>↗</sup></button>; }
function SectionHead({ eyebrow, title, note, action }: { eyebrow: string; title: string; note?: string; action?: ReactNode }) { return <header className="section-head"><div><p className="eyebrow">{eyebrow}</p><h2>{title}</h2>{note && <p>{note}</p>}</div>{action}</header>; }

function MiniLine({ primary, benchmark, compact=false }: { primary: number[]; benchmark: number[]; compact?: boolean }) {
  const width = 640, height = compact ? 170 : 235, pad = 16;
  const all = [...primary,...benchmark], min = Math.min(...all)-3, max = Math.max(...all)+3;
  const points = (data:number[]) => data.map((v,i)=>`${pad+(i*(width-pad*2))/(data.length-1)},${height-pad-((v-min)/(max-min))*(height-pad*2)}`).join(" ");
  const endY = Number(points(primary).split(" ").at(-1)?.split(",")[1]);
  return <svg className="line-chart" viewBox={`0 0 ${width} ${height}`} role="img" aria-label="Portfolio and benchmark performance chart">
    {[.2,.4,.6,.8].map((p)=><line key={p} x1="16" x2="624" y1={height*p} y2={height*p} className="grid-line"/>)}
    <polyline points={points(benchmark)} className="benchmark-line"/><polyline points={points(primary)} className="portfolio-line"/>
    <circle cx={width-pad} cy={endY} r="4" className="end-dot"/>
  </svg>;
}

function Bar({ label, value, max=100, suffix="", tone="green" }: { label:string; value:number; max?:number; suffix?:string; tone?:string }) {
  return <div className="bar-row"><div><span>{label}</span><b>{value}{suffix}</b></div><section><i className={tone} style={{width:`${Math.min(100,(value/max)*100)}%`}}/></section></div>;
}

async function postJson<T>(url:string, body:unknown):Promise<T> {
  const response = await fetch(url,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(body)});
  const payload = await response.json() as T & {error?:string};
  if(!response.ok) throw new Error(payload.error || `Request failed with HTTP ${response.status}.`);
  return payload;
}

function Onboarding({ onDone, onPreview }: { onDone:(profile:InvestorProfile)=>void; onPreview:()=>void }) {
  const [step,setStep] = useState(0);
  const [answers,setAnswers] = useState<InvestorProfile>({goal:"Long-term growth",budget:"$25,000",horizon:"3–5 years",loss:"Up to 20%",reaction:"Hold and review",level:"Intermediate — connect data and ideas",rules:["Keep at least 10% cash"],custom:""});
  const q = questions[step], selected = answers[q.id];
  const choose = (option:string) => {
    if(q.id === "rules") { const list = (answers.rules as string[]) || []; setAnswers({...answers,rules:list.includes(option)?list.filter(x=>x!==option):[...list,option]}); }
    else setAnswers({...answers,[q.id]:option});
  };
  return <main className="onboarding"><section className="onboarding-intro"><Logo/><div><p className="eyebrow pale">A SIMPLE PLACE TO START</p><h1>Tell us what matters.<br/><em>We explain</em> the rest.</h1><p>A few short questions help turn your goal, time horizon and comfort with losses into a plan you can understand.</p></div><ol><li className={step>=0?"active":""}><span>1</span>Your goal</li><li className={step>=3?"active":""}><span>2</span>Your comfort</li><li className={step>=6?"active":""}><span>3</span>Your plan</li></ol><footer><span>Plain-language guidance</span><span>Your answers stay private</span><span>About 2 minutes</span></footer></section>
    <section className="question-panel"><header><button onClick={()=>setStep(Math.max(0,step-1))} disabled={step===0}>← Back</button><span>Your preferences <b>{step+1} / {questions.length}</b></span><button onClick={onPreview}>Preview an example</button></header><div className="progress"><i style={{width:`${((step+1)/questions.length)*100}%`}}/></div><div className="question-content"><p className="eyebrow">{q.step}</p><h2>{q.title}</h2><p>{q.note}</p>
    {q.id === "budget" ? <div className="budget-box">
      <label><span>Investable amount</span><b>{answers.budget as string}</b></label>
      <input type="range" min="5000" max="100000" step="5000" value={Number(String(answers.budget).replace(/[^0-9.]/g,""))||25000} onInput={e=>setAnswers({...answers,budget:`$${Number(e.currentTarget.value).toLocaleString()}`})}/>
      <div><span>$5k</span><span>$50k</span><span>$100k</span></div>
      <article><b>Safety check</b><p>Emergency savings and debt payments should be considered before investing.</p></article>
    </div> : <div className="answer-grid">{q.options.map(option=><button key={option} className={(Array.isArray(selected)?selected.includes(option):selected===option)?"selected":""} onClick={()=>choose(option)}><i>{(Array.isArray(selected)?selected.includes(option):selected===option)?"✓":"→"}</i><span>{option}</span></button>)}{q.id==="rules"&&<label><span>Custom instruction</span><input value={String(answers.custom || "")} onChange={e=>setAnswers({...answers,custom:e.target.value})} placeholder="e.g. Replace stock B with stock D"/></label>}</div>}</div>
    <footer><div><b>{step+1===questions.length?"Ready to see your plan":"You can change this later"}</b><span>{step+1===questions.length?"We will explain the result in plain English.":"The plan can be updated when your needs change."}</span></div><button onClick={()=>step===questions.length-1?onDone(answers):setStep(step+1)}>{step===questions.length-1?"See my plan":"Continue"} →</button></footer></section></main>;
}

export default function Home() {
  const [stage,setStage] = useState<Stage>("questionnaire"); const [activeTab,setActiveTab] = useState<Tab>("overview");
  const [company,setCompany] = useState<CompanyTicker>("AAPL"); const [rebalanced,setRebalanced] = useState(false);
  const [messages,setMessages] = useState<Message[]>([{role:"assistant",text:"Your portfolio is ready. Ask about a result, review the reasoning, or change a constraint."}]);
  const [draft,setDraft] = useState(""); const [selectedTerm,setSelectedTerm] = useState<GlossaryTerm | null>(null);
  const [chatExpanded,setChatExpanded] = useState(false);
  const [profile,setProfile] = useState<InvestorProfile>({goal:"Long-term growth",budget:"$25,000",horizon:"3–5 years",loss:"Up to 20%",reaction:"Hold and review",level:"Intermediate — connect data and ideas",rules:["Keep at least 10% cash"],custom:""});
  const [apiPanel,setApiPanel] = useState(false); const [glmKey,setGlmKey] = useState(""); const [alphaKey,setAlphaKey] = useState("");
  const [symbolsInput,setSymbolsInput] = useState("AAPL, MSFT, NVDA"); const [model,setModel] = useState("glm-5.2"); const [privateReport,setPrivateReport] = useState("");
  const [apiStatus,setApiStatus] = useState<ApiStatus>("demo"); const [apiError,setApiError] = useState(""); const [chatBusy,setChatBusy] = useState(false);
  const [isRunning,setIsRunning] = useState(false); const requestVersion = useRef(0);
  const autoRefreshStarted = useRef(false); const runLiveRef = useRef<(options?:{symbols?:string[];portfolio?:Holding[]})=>Promise<void>>(async()=>{});
  const [liveMarket,setLiveMarket] = useState<LiveMarket | null>(null); const [liveAnalysis,setLiveAnalysis] = useState<LiveAnalysis | null>(null);
  const holdings = rebalanced ? revisedHoldings : initialHoldings;
  const investableAmount = Number(String(profile.budget||"25000").replace(/[^0-9.]/g,""))||25000;
  const donut = useMemo(()=>{let start=0; const segments=holdings.map(h=>{const end=start+h.weight;const item=`${h.color} ${start}% ${end}%`;start=end;return item;});return `conic-gradient(${segments.join(",")})`;},[holdings]);
  const showDashboard = () => {setStage("dashboard");window.requestAnimationFrame(()=>window.scrollTo({top:0,left:0}));};
  const enter = () => {setStage("building");window.setTimeout(showDashboard,950);};
  const answer = (question:string,text:string,tab?:Tab) => {setMessages(m=>[...m,{role:"user",text:question},{role:"assistant",text}]);if(tab)setActiveTab(tab);};
  const explain = (term:GlossaryTerm) => {setSelectedTerm(term);answer(`Explain ${term} in this analysis.`,`${glossary[term][0]} ${glossary[term][1]}`);};
  const runLiveAnalysis = async (options?:unknown) => {
    const runOptions=options&&typeof options==="object"&&("symbols" in options||"portfolio" in options)?options as {symbols?:string[];portfolio?:Holding[]}:undefined;
    const requestId=++requestVersion.current;
    const symbols=runOptions?.symbols||symbolsInput.split(/[\s,]+/).map(x=>x.trim().toUpperCase()).filter(Boolean).slice(0,3);
    const targetHoldings=runOptions?.portfolio||holdings;
    if(!symbols.length){setApiPanel(true);setApiError("Add at least one ticker to update the market data.");return;}
    const requestedMode=alphaKey.trim()?"alpha" as const:"public" as const;
    setIsRunning(true);
    setApiStatus("loading");setApiError("");setApiPanel(false);showDashboard();setActiveTab("overview");
    try{
      const sameSymbols=Boolean(liveMarket)&&liveMarket!.symbols.join(",")===symbols.join(",");
      const sameSource=Boolean(liveMarket)&&liveMarket!.dataMode===requestedMode;
      const snapshotAge=liveMarket?Date.now()-Date.parse(liveMarket.fetchedAt):Number.POSITIVE_INFINITY;
      const reuseMarket=Boolean(liveMarket)&&liveMarket!.dataStatus!=="stale"&&sameSymbols&&sameSource&&snapshotAge<15*60*1000;
      const market=reuseMarket
        ? {...liveMarket!,servedAt:new Date().toISOString(),dataStatus:liveMarket!.dataStatus==="stale"?"stale" as const:"cached" as const,usage:{...liveMarket!.usage,providerRequests:0,alphaRequests:liveMarket!.usage.alphaRequests===undefined?undefined:0}}
        : requestedMode==="alpha"
          ? await postJson<LiveMarket>("/api/market",{alphaVantageKey:alphaKey,symbols})
          : await postJson<LiveMarket>("/api/public-market",{symbols});
      if(requestId!==requestVersion.current)return;
      setLiveMarket(market);
      setLiveAnalysis(null);setApiStatus("live");
      try{
        const result=await postJson<{analysis:LiveAnalysis}>("/api/analyze",{glmKey,model,mode:"analyze",market,profile:{...profile,privateReport:privateReport.slice(0,12000)},holdings:targetHoldings});
        if(requestId!==requestVersion.current)return;
        setLiveAnalysis(result.analysis);
        const sourceNote=market.dataStatus==="stale"?" The explanation uses the last available, clearly dated information.":"";
        setMessages(m=>[...m,{role:"assistant",text:`Analysis complete. ${result.analysis.summary}${sourceNote}`}]);
      }catch(error){
        if(requestId!==requestVersion.current)return;
        const message=error instanceof Error?error.message:"AI analysis failed.";
        if(/Add a GLM API key/i.test(message)){
          setMessages(m=>[...m,{role:"assistant",text:"Prices and company updates are ready. A new explanation has not been generated, so no new recommendation is being shown."}]);
        }else{
          setApiError("Prices and company updates are ready, but a new explanation could not be generated. You can still review the dated information.");
          setMessages(m=>[...m,{role:"assistant",text:"Prices and company updates are ready, but a new explanation could not be generated. The dated information is still available to review."}]);
        }
      }
    }catch(error){
      if(requestId!==requestVersion.current)return;
      void error;
      setApiError("We couldn't refresh prices and company updates just now. Please try again in a moment.");setApiStatus(liveMarket?"live":"error");
    }finally{if(requestId===requestVersion.current)setIsRunning(false);}
  };
  const replace = () => {
    const nextSymbols=["AAPL","AMZN","NVDA"];
    requestVersion.current+=1;
    setRebalanced(true);setLiveAnalysis(null);setLiveMarket(null);setApiStatus("demo");setIsRunning(false);
    setSymbolsInput(nextSymbols.join(", "));setCompany("AMZN");
    answer("Replace Microsoft with Amazon.","This teaching example now shows AMZN at 30% instead of MSFT. The 15% cash cushion and 20% NVIDIA limit stay the same. Any earlier generated conclusion was cleared because the holdings changed. Prices and company updates are now being refreshed for the new list.");
    void runLiveAnalysis({symbols:nextSymbols,portfolio:revisedHoldings});
  };
  useEffect(()=>{runLiveRef.current=runLiveAnalysis;});
  useEffect(()=>{
    if(stage==="dashboard"&&apiStatus==="demo"&&!autoRefreshStarted.current){
      autoRefreshStarted.current=true;
      void runLiveRef.current();
    }
  },[stage,apiStatus]);
  const submit = async (e:FormEvent) => {
    e.preventDefault();const q=draft.trim();if(!q||chatBusy)return;setDraft("");
    const term=(Object.keys(glossary) as GlossaryTerm[]).find(k=>q.toLowerCase().includes(k.toLowerCase()));if(term){explain(term);return;}
    if(/replace|swap|amazon|amzn|microsoft|msft/i.test(q)){replace();return;}
    if(liveMarket){
      setMessages(m=>[...m,{role:"user",text:q}]);setChatBusy(true);
      try{const result=await postJson<{answer:string}>("/api/analyze",{glmKey,model,mode:"chat",market:liveMarket,profile:{...profile,privateReport:privateReport.slice(0,12000)},holdings,priorAnalysis:liveAnalysis,question:q});setMessages(m=>[...m,{role:"assistant",text:result.answer}]);}
      catch(error){void error;setMessages(m=>[...m,{role:"assistant",text:"I couldn't create a fresh answer just now. The dated prices and company updates are still available to review."}]);}
      finally{setChatBusy(false);}return;
    }
    if(/company|apple|aapl|nvidia|nvda/i.test(q)){answer(q,"Open Company Overview for price context, operating quality, valuation, relative performance, thesis, risk and catalysts.","company");return;}
    if(/agent|debate|bull|bear|analyst/i.test(q)){answer(q,"The Agent Room shows each specialist report, the Bull–Bear debate, Trader proposal, three risk perspectives and the Portfolio Manager decision.","agents");return;}
    answer(q,"Refresh prices and news first. If a fresh explanation is unavailable, the dated market information can still be reviewed on its own.");
  };

  if(stage==="questionnaire") return <Onboarding onDone={nextProfile=>{requestVersion.current+=1;setIsRunning(false);setProfile(nextProfile);setRebalanced(false);setLiveMarket(null);setLiveAnalysis(null);setApiStatus("demo");setSymbolsInput("AAPL, MSFT, NVDA");autoRefreshStarted.current=false;enter();}} onPreview={showDashboard}/>;
  if(stage==="building") return <main className="building"><Logo/><div className="build-orbit"><span>PRICES</span><span>BUSINESSES</span><span>UPDATES</span><span>RISK</span><b>C</b></div><h1>Preparing your plan</h1><p>Your answers → example mix → downside check</p></main>;
  return <><div className={`app-shell ${chatExpanded?"chat-open":"chat-closed"}`}><aside className="chat-rail"><div className="chat-brand"><Logo/><button onClick={()=>setChatExpanded(open=>!open)} aria-expanded={chatExpanded} aria-label={chatExpanded?"Collapse portfolio guide":"Open portfolio guide"}>{chatExpanded?"Close":"Ask"}</button></div><div className="assistant-status"><span>C</span><div><b>Portfolio guide</b><small><i/> {liveMarket?liveMarket.dataStatus==="stale"?"Last available information":"Prices and news ready":"Example plan"}</small></div></div><div className="chat-context"><span>VIEWING</span><b>{tabs.find(t=>t.id===activeTab)?.label}</b><em>{rebalanced?"Updated by you":"Original plan"}</em></div><div className="messages">{messages.map((m,i)=><div className={`message ${m.role}`} key={i}>{m.role==="assistant"&&<span>C</span>}<p>{m.text}</p></div>)}{chatBusy&&<div className="message assistant"><span>C</span><p>Reviewing the evidence…</p></div>}{selectedTerm&&<article className="term-card"><small>DEFINITION</small><b>{selectedTerm}</b><p>{glossary[selectedTerm][0]}</p><button onClick={()=>setActiveTab(selectedTerm==="ROA"||selectedTerm==="P/E ratio"||selectedTerm==="Gross margin"?"company":"risk")}>See it in context →</button></article>}</div><div className="quick-prompts"><button onClick={()=>explain("Max drawdown")}>Explain a possible loss</button><button onClick={()=>setActiveTab("agents")}>Why these stocks?</button><button onClick={replace}>I prefer Amazon</button></div><form onSubmit={submit}><textarea value={draft} onChange={e=>setDraft(e.target.value)} placeholder="Tell me an idea, for example: I do not want chip stocks"/><button aria-label="Send message" disabled={chatBusy}>↑</button><small>For learning, not financial advice</small></form></aside>
  <main className="workspace"><header className="topbar"><Logo/><div className={`snapshot ${apiStatus}`}><span><i/> {isRunning?"Checking prices & news":apiStatus==="error"?"Refresh didn't finish":liveMarket?liveMarket.dataStatus==="stale"?"Showing last available data":liveMarket.dataMode==="public"?"Delayed closing prices":"Prices and news updated":"Preparing latest information"}</span><b>{liveMarket?`Prices through ${marketDate(liveMarket.quotes[0]?.latestTradingDay||"")}`:apiStatus==="error"?"Try again when ready":"No refresh completed yet"}</b></div><div className="api-actions"><button className="run-live" onClick={runLiveAnalysis} disabled={isRunning}>{isRunning?"Refreshing…":"Refresh prices & news"}</button></div><div className="profile-pill"><span>SG</span><div><b>{String(profile.goal||"Growth learner")}</b><small>{String(profile.loss||"Moderate risk")} · {String(profile.horizon||"3–5 years")}</small></div><button onClick={()=>setStage("questionnaire")}>Edit preferences</button></div></header><nav className="tabs" aria-label="Portfolio sections">{tabs.map(t=><button key={t.id} className={activeTab===t.id?"active":""} onClick={()=>setActiveTab(t.id)}><small>{t.number}</small>{t.label}{t.id==="events"&&liveMarket&&<i aria-label={`${liveMarket.news.length} matched updates`}>{liveMarket.news.length}</i>}</button>)}</nav><div className="page-content">{activeTab==="overview"&&<Overview holdings={holdings} donut={donut} amount={investableAmount} rebalanced={rebalanced} explain={explain} go={setActiveTab} replace={replace} liveMarket={liveMarket} liveAnalysis={liveAnalysis} apiStatus={apiStatus} apiError={apiError} runLive={runLiveAnalysis}/>} {activeTab==="company"&&<CompanyView ticker={company} setTicker={setCompany} amount={investableAmount} rebalanced={rebalanced} explain={explain} ask={answer} liveMarket={liveMarket}/>} {activeTab==="agents"&&<AgentRoom amount={investableAmount} rebalanced={rebalanced} explain={explain} ask={answer} liveAnalysis={liveAnalysis} marketUpdated={Boolean(liveMarket)}/>} {activeTab==="events"&&<EventsView ask={answer} liveMarket={liveMarket}/>} {activeTab==="risk"&&<RiskView amount={investableAmount} lossPreference={String(profile.loss||"Not set")} rebalanced={rebalanced} explain={explain} ask={answer}/>} {activeTab==="evidence"&&<EvidenceView rebalanced={rebalanced} explain={explain} ask={answer} openSettings={()=>setApiPanel(true)}/>}</div></main></div><ApiSetup open={apiPanel} onClose={()=>setApiPanel(false)} glmKey={glmKey} setGlmKey={setGlmKey} alphaKey={alphaKey} setAlphaKey={setAlphaKey} symbols={symbolsInput} setSymbols={setSymbolsInput} model={model} setModel={setModel} privateReport={privateReport} setPrivateReport={setPrivateReport} error={apiError} onRun={runLiveAnalysis} busy={isRunning}/></>;
}

function ApiSetup({open,onClose,glmKey,setGlmKey,alphaKey,setAlphaKey,symbols,setSymbols,model,setModel,privateReport,setPrivateReport,error,onRun,busy}:{open:boolean;onClose:()=>void;glmKey:string;setGlmKey:(v:string)=>void;alphaKey:string;setAlphaKey:(v:string)=>void;symbols:string;setSymbols:(v:string)=>void;model:string;setModel:(v:string)=>void;privateReport:string;setPrivateReport:(v:string)=>void;error:string;onRun:()=>void;busy:boolean}) {
  if(!open)return null;
  return <div className="modal-backdrop" role="presentation" onMouseDown={e=>e.target===e.currentTarget&&onClose()}><section className="api-modal" role="dialog" aria-modal="true" aria-labelledby="api-title"><header><div><p className="eyebrow">SETTINGS</p><h2 id="api-title">Choose what to follow</h2></div><button onClick={onClose} aria-label="Close settings">×</button></header><p className="api-intro">The site can show dated closing prices and official company updates without any paid connection. The options below are only for the project demo.</p><label><span>Stocks to follow · maximum 3</span><input value={symbols} onChange={e=>setSymbols(e.target.value)} placeholder="AAPL, MSFT, NVDA"/></label><details className="connection-settings"><summary>Optional project connections</summary><label><span>GLM API key</span><input type="password" autoComplete="off" value={glmKey} onChange={e=>setGlmKey(e.target.value)} placeholder="Your Zhipu API key"/><small>Creates a fresh plain-language analysis. The key stays in this browser tab.</small></label><label><span>Explanation model</span><select value={model} onChange={e=>setModel(e.target.value)}><option value="glm-5.2">GLM-5.2</option><option value="glm-5.3-flash">GLM-5.3 Flash</option><option value="glm-5.3">GLM-5.3</option></select></label><label><span>Alpha Vantage API key</span><input type="password" autoComplete="off" value={alphaKey} onChange={e=>setAlphaKey(e.target.value)} placeholder="Leave blank for public data"/><small>Optional. The normal refresh button works without it.</small></label><label><span>Private analyst note</span><textarea value={privateReport} onChange={e=>setPrivateReport(e.target.value)} placeholder="Paste an optional note for the project demo."/><small>This note is treated as private context, not as a public fact.</small></label></details>{error&&<p className="api-error">{error}</p>}<div className="api-safety"><b>What users see</b><p>Every price and company update shows its own date. Delayed information is labeled clearly.</p></div><footer><button onClick={onClose}>Keep current view</button><button className="primary" onClick={onRun} disabled={busy}>{busy?"Refreshing…":glmKey.trim()?"Refresh & explain →":"Refresh prices & news →"}</button></footer></section></div>;
}

function money(value:number){if(!value)return "—";if(value>=1e12)return `$${(value/1e12).toFixed(2)}T`;if(value>=1e9)return `$${(value/1e9).toFixed(1)}B`;return `$${value.toLocaleString()}`;}
function wholeMoney(value:number){return new Intl.NumberFormat("en-US",{style:"currency",currency:"USD",maximumFractionDigits:0}).format(value);}
function percent(value:number){return value?`${(value*100).toFixed(1)}%`:"—";}
function newsDate(value:string){const date=new Date(value);return Number.isNaN(date.getTime())?"Time unavailable":new Intl.DateTimeFormat("en-US",{month:"short",day:"numeric",year:"numeric",hour:"2-digit",minute:"2-digit",hour12:false,timeZone:"UTC",timeZoneName:"short"}).format(date);}
function marketDate(value:string){const date=new Date(`${value}T00:00:00Z`);return !value||Number.isNaN(date.getTime())?"latest close":new Intl.DateTimeFormat("en-US",{month:"short",day:"numeric",year:"numeric",timeZone:"UTC"}).format(date);}
function newsDateParts(value:string){const date=new Date(value);return Number.isNaN(date.getTime())?{day:"—",month:"UTC"}:{day:String(date.getUTCDate()).padStart(2,"0"),month:date.toLocaleDateString("en-US",{month:"short",timeZone:"UTC"}).toUpperCase()};}
function newsBasisLabel(value:"published"|"updated"|"filed"|"mixed"){return value==="updated"?"Updated":value==="filed"?"Filed":value==="mixed"?"Published / filed":"Published";}

function LiveWorkspace({market,analysis,status,onRun}:{market:LiveMarket|null;analysis:LiveAnalysis|null;status:ApiStatus;onRun:()=>void}) {
  if(status==="demo")return <section className="connect-banner card"><div><h3>Check the latest available prices and company updates</h3><p>Closing prices are delayed and always show their date.</p></div><button onClick={onRun}>Refresh now →</button></section>;
  if(status==="loading")return <section className="live-loading card"><span/><div><h3>Checking prices and recent company updates</h3><p>This may take a few seconds.</p></div></section>;
  if(!market)return null;
  const latestClose=market.quotes.map(q=>q.latestTradingDay).filter(Boolean).sort().at(-1)||"";
  const statusLabel=market.dataStatus==="stale"?"LAST AVAILABLE":market.dataMode==="public"?"DELAYED PRICES":"UPDATED";
  const statusText=market.dataStatus==="stale"
    ? "We could not refresh just now, so the last clearly dated information is shown."
    : `Closing prices are shown through ${marketDate(latestClose)}.`;
  return <section className={`live-workspace ${market.dataStatus}`}><header><div><h2>Current prices and company updates</h2><p>{statusText}</p></div><span>{statusLabel}</span></header>{market.warning&&<p className="live-warning">Some information could not be refreshed. Every visible item still shows its own date.</p>}<div className="live-quotes">{market.quotes.map(q=><article className="card" key={q.symbol}><span>{q.symbol}</span><b>{q.price?`$${q.price.toFixed(2)}`:"—"}</b><em className={q.change>=0?"positive":"negative"}>{q.change>=0?"Up ":"Down "}{q.changePercent.replace(/[+-]/,"")}</em><small>{q.isRealtime?"Real-time price":"Latest available close"} · {marketDate(q.latestTradingDay)}</small></article>)}</div><div className="live-grid"><article className="card live-company"><p className="eyebrow">COMPANY AT A GLANCE</p><h3>{market.overview.name}</h3><p>{market.overview.sector} · {market.overview.industry}</p><dl><div><dt>Company value</dt><dd>{money(market.overview.marketCapitalization)}</dd></div><div><dt>Price vs earnings</dt><dd>{market.overview.peRatio?`${market.overview.peRatio.toFixed(1)}×`:"—"}</dd></div><div><dt>Return on assets</dt><dd>{percent(market.overview.returnOnAssets)}</dd></div><div><dt>Profit margin</dt><dd>{percent(market.overview.profitMargin)}</dd></div><div><dt>Revenue growth</dt><dd>{percent(market.overview.revenueGrowth)}</dd></div><div><dt>Analyst target</dt><dd>{market.overview.analystTargetPrice?`$${market.overview.analystTargetPrice.toFixed(2)}`:"—"}</dd></div></dl></article><article className="card live-decision"><p className="eyebrow">PLAIN-LANGUAGE SUMMARY</p><h3>{analysis?.decision.label||"No new recommendation"}</h3>{analysis&&<div className="live-confidence"><b>{Math.round(analysis.confidence)}</b><span>/ 100<br/>INFORMATION STRENGTH</span></div>}<p>{analysis?.summary||"Prices and company updates are ready to review. This example has not been re-evaluated, so the page does not show a new buy or sell view."}</p>{analysis&&<><blockquote>{analysis.decision.rationale}</blockquote><small>CHECK AGAIN WHEN · {analysis.decision.reviewTrigger}</small></>}</article><article className="card live-news"><p className="eyebrow">RECENT COMPANY UPDATES</p><h3>What may change the investment case</h3>{market.news.slice(0,4).map(item=><a key={item.url||item.title} href={item.url||undefined} target="_blank" rel="noreferrer"><span>{item.symbols.join(" · ")}</span><b>{item.title}</b><small>{item.source} · {newsBasisLabel(item.timeBasis||market.newsTimeBasis)} {newsDate(item.publishedAt)}</small></a>)}</article></div>{analysis&&<div className="live-risk card"><div><p className="eyebrow">WHY IT MAY WORK</p>{analysis.bullCase.slice(0,3).map(x=><span key={x}>+ {x}</span>)}</div><div><p className="eyebrow">WHY IT MAY NOT</p>{analysis.bearCase.slice(0,3).map(x=><span key={x}>− {x}</span>)}</div><div><p className="eyebrow">WHAT TO WATCH</p>{analysis.riskFlags.slice(0,3).map(x=><span key={x}>! {x}</span>)}</div></div>}</section>;
}

function Overview({holdings,donut,amount,rebalanced,explain,go,replace,liveMarket,liveAnalysis,apiStatus,apiError,runLive}:{holdings:Holding[];donut:string;amount:number;rebalanced:boolean;explain:(t:GlossaryTerm)=>void;go:(t:Tab)=>void;replace:()=>void;liveMarket:LiveMarket|null;liveAnalysis:LiveAnalysis|null;apiStatus:ApiStatus;apiError:string;runLive:()=>void}) {
  const portfolio=[100,102,104,107,111,110,116,119,123,127,126,131], benchmark=[100,101,103,104,107,108,110,111,113,114,116,118];
  const awaitingAnalysis=Boolean(liveMarket&&!liveAnalysis);
  const quoteDateRaw=liveMarket?.quotes.map(q=>q.latestTradingDay).filter(Boolean).sort().at(-1)||"";
  const quoteDate=quoteDateRaw?marketDate(quoteDateRaw):"Not checked yet";
  const newestEvent=liveMarket?.news[0]?.publishedAt;
  const newestEventLabel=newestEvent?new Intl.DateTimeFormat("en-US",{month:"short",day:"numeric",year:"numeric",timeZone:"UTC"}).format(new Date(newestEvent)):"No matched event";
  const checkedLabel=liveMarket?new Intl.DateTimeFormat("en-US",{month:"short",day:"numeric",hour:"2-digit",minute:"2-digit",timeZone:"UTC",timeZoneName:"short"}).format(new Date(liveMarket.freshness.newsFetchedAt)):"Starting now";
  return <>
    <section className="beginner-hero">
      <div className="beginner-hero-copy">
        <span className="plain-kicker">START HERE · EXAMPLE PLAN</span>
        <h1>A growth-focused portfolio,<br/><em>explained in plain English.</em></h1>
        <p>This example puts most of the money in three large technology companies and keeps 15% in cash. It may grow faster than the broad market, but it can also fall more sharply.</p>
        <div className="hero-actions"><button onClick={()=>go("company")}>See what I would own</button><button onClick={()=>go("risk")}>See what could go wrong</button></div>
        <small>This is an educational walkthrough, not a personalized recommendation or a promise of returns.</small>
      </div>
      <aside className={liveAnalysis?"has-analysis":"no-analysis"}>
        <span>{liveAnalysis?"CURRENT VIEW":"TODAY'S STATUS"}</span>
        <h2>{liveAnalysis?liveAnalysis.decision.label:awaitingAnalysis?"Prices and company updates are ready. This example has not been re-evaluated.":"The latest available information is being checked automatically."}</h2>
        <p>{liveAnalysis?liveAnalysis.summary:"You can explore the example allocation now. A new explanation is optional and will never be confused with the dated market information."}</p>
        {liveAnalysis&&<dl><div><dt>Information strength</dt><dd>{Math.round(liveAnalysis.confidence)} / 100</dd></div><div><dt>Check again when</dt><dd>{liveAnalysis.decision.reviewTrigger}</dd></div></dl>}
        {liveAnalysis&&<small>Information strength is not the probability of making money.</small>}
      </aside>
    </section>

    <section className={`freshness-card ${liveMarket?.dataStatus||apiStatus}`} aria-live="polite">
      <div className="freshness-title"><i/><span><b>{apiStatus==="loading"?"Checking the latest information…":liveMarket?"Data check complete":"Waiting to check data"}</b><small>{liveMarket?liveMarket.dataMode==="public"?"Delayed closing prices":"Updated prices":"No result yet"}</small></span></div>
      <div><small>Latest market close</small><b>{quoteDate}</b><span>Prices are not real time</span></div>
      <div><small>Newest matched update</small><b>{newestEventLabel}</b><span>Updates checked {checkedLabel}</span></div>
      <button onClick={runLive} disabled={apiStatus==="loading"}>{apiStatus==="loading"?"Refreshing…":"Refresh now"}</button>
      <p className={apiStatus==="error"?"refresh-error":""}>{apiStatus==="error"?apiError:"“Latest market close” can be earlier than today on weekends and market holidays. The update date and the time we checked for newer information are shown separately."}</p>
    </section>

    {apiStatus==="loading"?<LiveWorkspace market={liveMarket} analysis={liveAnalysis} status={apiStatus} onRun={runLive}/>:<details className="data-details"><summary><span><b>See the current prices, events and sources</b><small>Open this when you want to inspect the evidence behind the dates above.</small></span><strong>View data</strong></summary><LiveWorkspace market={liveMarket} analysis={liveAnalysis} status={apiStatus} onRun={runLive}/></details>}

    <section className="beginner-grid">
      <article className="card allocation-card beginner-allocation"><SectionHead eyebrow="ALLOCATION" title={`Where the example ${wholeMoney(amount)} goes`} note={rebalanced?"Updated in the preference-change example":"Tap a stock to understand why it is included"}/><div className="allocation-body"><div className="donut" style={{background:donut}}><div><b>85%</b><span>in stocks</span></div></div><div>{holdings.map(h=><button key={h.ticker} onClick={()=>h.ticker!=="CASH"&&go("company")}><i style={{background:h.color}}/><span><b>{h.ticker}</b><small>{h.name}</small></span><em>{wholeMoney(amount*h.weight/100)}</em><strong>{h.weight}%</strong></button>)}</div></div><footer><b>Main thing to understand</b><span>Different company names do not remove the shared dependence on large technology stocks.</span></footer></article>
      <article className="card plain-risk-card"><span className="demo-badge">EXAMPLE CALCULATION</span><h2>What could this feel like?</h2><div><small>Example historical result</small><b className="positive">+{wholeMoney(amount*.184)}</b><span>+18.4% on {wholeMoney(amount)}</span></div><div className="downside"><small>Biggest example fall</small><b>−{wholeMoney(amount*.142)}</b><span>−14.2% on {wholeMoney(amount)}</span></div><p>Past gains and falls do not predict the future. Dollar amounts make the trade-off easier to picture.</p><button onClick={()=>go("risk")}>Explore bad-market scenarios →</button></article>
    </section>

    <section className="plain-next card"><SectionHead eyebrow="NEXT STEPS" title="Three useful things you can do next" note="You do not need to understand every ratio before continuing."/><div><button onClick={()=>go("company")}><span>1</span><b>Understand each stock</b><small>What it does, why it is here, and its main risk.</small><i>→</i></button><button onClick={()=>go("events")}><span>2</span><b>See what changed recently</b><small>Read dated company updates from the original sources.</small><i>→</i></button><button onClick={()=>go("risk")}><span>3</span><b>Check whether the downside feels acceptable</b><small>Translate percentages into possible dollar losses.</small><i>→</i></button></div><footer><span>Have a different preference?</span><button onClick={replace}>{rebalanced?"Example applied: MSFT → AMZN":"See an example: replace MSFT"}</button></footer></section>

    <details className="advanced-details">
      <summary><span><b>Want the financial details?</b><small>Open the example performance charts, market comparisons and advanced ratios.</small></span><strong>Show advanced details</strong></summary>
      <div className="demo-disclosure"><b>Demo calculations</b><span>The figures below are fixed teaching examples. They do not update when the live price and news feed refreshes.</span></div>
      <section className="metric-strip"><article><small>Example amount</small><b>{wholeMoney(amount)}</b><span>Investable capital</span></article><article><small>Example YTD return</small><b className="positive">+18.4%</b><span>vs S&amp;P 500 +12.6%</span></article><article><small><Learn term="Beta" explain={explain}/></small><b>1.16</b><span>Historically moved about 16% more than the market</span></article><article><small><Learn term="Max drawdown" explain={explain}/></small><b>−14.2%</b><span>Biggest example peak-to-trough fall</span></article><article><small>Cash cushion</small><b>15.0%</b><span>Above the 10% example minimum</span></article></section>
      <section className="overview-grid advanced-grid"><article className="card performance-card"><SectionHead eyebrow="RELATIVE PERFORMANCE" title="Example portfolio vs S&amp;P 500" note="Normalized illustrative total-return index · Jan–Aug 2026" action={<div className="legend"><span><i className="green"/>Portfolio</span><span><i className="gray"/>S&amp;P 500</span></div>}/><MiniLine primary={portfolio} benchmark={benchmark}/><div className="chart-axis"><span>JAN</span><span>MAR</span><span>MAY</span><span>JUL</span><span>AUG</span></div><div className="comparison-footer"><div><b>+5.8 pp</b><span>More than the market</span></div><div><b>18.6%</b><span>Annualized ups and downs</span></div><div><b><Learn term="Sharpe ratio" explain={explain}/></b><span>1.18 vs 0.93</span></div><div><b><Learn term="Tracking error" explain={explain}/></b><span>8.4%</span></div></div></article>
      <article className="card benchmark-card"><SectionHead eyebrow="BENCHMARK GAP" title="How this differs from owning the broad market" note="Percentage-point difference from the S&amp;P 500"/><div className="diverging"><div><span>Technology</span><section><i className="plus" style={{width:"92%"}}/></section><b>+62 pp</b></div><div><span>Cash</span><section><i className="plus" style={{width:"24%"}}/></section><b>+12 pp</b></div><div><span>Financials</span><section><i className="minus" style={{width:"20%"}}/></section><b>−13 pp</b></div><div><span>Health care</span><section><i className="minus" style={{width:"18%"}}/></section><b>−11 pp</b></div><div><span>Other sectors</span><section><i className="minus" style={{width:"41%"}}/></section><b>−38 pp</b></div></div><p className="insight"><b>In plain English:</b> this is a focused learning portfolio, not a replacement for the whole stock market.</p></article>
      <article className="card attribution-card"><SectionHead eyebrow="RETURN ATTRIBUTION" title="What drove the example result" note="Illustrative YTD contribution to +18.4%"/><div className="attribution"><div><span>NVDA</span><section><i style={{width:"88%"}}/></section><b>+8.1%</b></div><div><span>{rebalanced?"AMZN":"MSFT"}</span><section><i style={{width:"52%"}}/></section><b>+4.8%</b></div><div><span>AAPL</span><section><i style={{width:"39%"}}/></section><b>+3.6%</b></div><div><span>Cash &amp; rebalance</span><section><i style={{width:"21%"}}/></section><b>+1.9%</b></div></div><footer><span>44%</span><p>of the sample gain came from one stock. A strong winner can also create a bigger concentration risk.</p></footer></article>
      <article className="card risk-return"><SectionHead eyebrow="RISK / RETURN MAP" title="Higher possible return usually means more uncertainty" note="Bubble size = example portfolio weight"/><div className="scatter"><span className="axis-y">Possible return →</span><span className="axis-x">Risk →</span><i className="quadrant q1"/><i className="quadrant q2"/><button className="bubble aapl" onClick={()=>go("company")}>AAPL</button><button className="bubble msft" onClick={()=>go("company")}>{rebalanced?"AMZN":"MSFT"}</button><button className="bubble nvda" onClick={()=>go("company")}>NVDA</button><button className="bubble cash">CASH</button><span className="market-dot">S&amp;P 500</span></div></article></section>
    </details>
    <p className="data-footnote prominent-footnote">Live prices and dated events appear only in the clearly labeled data section. Allocation, historical performance and risk figures are illustrative prototype calculations.</p>
  </>;
}

function CompanyView({ticker,setTicker,amount,rebalanced,explain,ask,liveMarket}:{ticker:CompanyTicker;setTicker:(t:CompanyTicker)=>void;amount:number;rebalanced:boolean;explain:(t:GlossaryTerm)=>void;ask:(q:string,a:string,t?:Tab)=>void;liveMarket:LiveMarket|null}) {
  const c=companies[ticker], options:CompanyTicker[]=rebalanced?["AAPL","AMZN","NVDA"]:["AAPL","MSFT","NVDA"];
  const live=liveMarket?.overview.symbol===ticker?liveMarket.overview:null, quote=liveMarket?.quotes.find(q=>q.symbol===ticker);
  const price=quote?.price?`$${quote.price.toFixed(2)}`:liveMarket?"—":c.price, day=quote?.changePercent||(!liveMarket?c.day:"—"), dayTone=quote?(quote.change>=0?"positive":"negative"):liveMarket?"":c.dayTone;
  const cap=live?money(live.marketCapitalization):liveMarket?"—":c.cap, pe=live?.peRatio?`${live.peRatio.toFixed(1)}×`:liveMarket?"—":c.pe, roa=live?percent(live.returnOnAssets):liveMarket?"—":c.roa;
  const revenue=live?percent(live.revenueGrowth):liveMarket?"—":c.revenue, margin=live?percent(live.profitMargin):liveMarket?"—":c.margin, eps=live?.eps?`$${live.eps.toFixed(2)}`:liveMarket?"—":c.eps;
  const weight=ticker==="AAPL"?35:ticker==="NVDA"?20:30;
  return <><SectionHead eyebrow="MY STOCKS" title="Understand why each stock is in the example." note="Start with four plain-language questions. Open the financial details only when you want them." action={<div className="company-switch">{options.map(t=><button className={ticker===t?"active":""} key={t} onClick={()=>setTicker(t)}>{t}</button>)}</div>}/><section className="company-simple card"><header><div className="company-identity"><span>{ticker.slice(0,1)}</span><div><small>{live?`${live.sector} · ${live.industry}`:c.sector}</small><h1>{live?.name||c.name}</h1></div></div><div><small>LATEST AVAILABLE PRICE</small><b>{price}</b><span className={dayTone}>{quote?`${quote.change>=0?"Up":"Down"} ${day.replace(/[+-]/,"")}`:day}</span><em>{quote?marketDate(quote.latestTradingDay):liveMarket?"Price unavailable":"Example price"}</em></div></header><div className="company-answers"><article><span>1</span><div><small>HOW MUCH OF THE EXAMPLE?</small><b>{weight}% · {wholeMoney(amount*weight/100)}</b><p>This is the share of the {wholeMoney(amount)} teaching portfolio.</p></div></article><article><span>2</span><div><small>WHY INCLUDE IT?</small><b>{ticker==="NVDA"?"Growth engine":"Core growth holding"}</b><p>{c.thesis}</p></div></article><article><span>3</span><div><small>WHAT IS THE MAIN RISK?</small><b>{ticker==="NVDA"?"High expectations":"Price and execution risk"}</b><p>{c.risk}</p></div></article><article><span>4</span><div><small>WHEN SHOULD IT BE CHECKED AGAIN?</small><b>After an important company update</b><p>{c.catalyst}</p></div></article></div></section><details className="advanced-details company-details"><summary><span><b>See the price chart and financial details</b><small>Company value, earnings, profitability, valuation and the example decision path.</small></span><strong>Show details</strong></summary><section className="company-hero card"><div className="company-identity"><span>{ticker.slice(0,1)}</span><div><p>{live?`${live.sector} · ${live.industry}`:c.sector}</p><h1>{live?.name||c.name}</h1><b>{c.verdict}</b></div></div><div className="company-price"><small>MARKET PRICE</small><b>{price}</b><span className={dayTone}>{day} session</span><em>{liveMarket?quote?`${marketDate(quote.latestTradingDay)} · delayed close`:"Updated price unavailable":"Example price"}</em></div><div className="company-head-metrics"><div><span>Company value</span><b>{cap}</b></div><div><Learn term="P/E ratio" explain={explain}/><b>{pe}</b></div><div><Learn term="ROA" explain={explain}/><b>{roa}</b></div></div></section>
  <section className="company-grid"><article className="card company-performance"><SectionHead eyebrow="RELATIVE PRICE PERFORMANCE" title={`${ticker} vs S&P 500`} note="Example 12-month comparison" action={<div className="legend"><span><i className="green"/>{ticker}</span><span><i className="gray"/>S&P 500</span></div>}/><MiniLine primary={c.performance} benchmark={c.benchmark}/><div className="chart-axis"><span>SEP</span><span>DEC</span><span>MAR</span><span>JUN</span><span>AUG</span></div></article><article className="card operating-card"><SectionHead eyebrow="OPERATING PERFORMANCE" title="Is the business improving?" note={live?"Updated company figures":liveMarket?"Updated figures are available for the first selected stock":"Example company figures"}/><div className="operating-metrics"><div><span>Revenue growth</span><b>{revenue}</b><i className="up">↗</i></div><div>{live?<span>Profit margin</span>:<Learn term="Gross margin" explain={explain}/>}<b>{margin}</b><i className="up">↗</i></div><div><span>Earnings per share</span><b>{eps}</b><i className="up">↗</i></div><div><Learn term="ROA" explain={explain}/><b>{roa}</b><i className="flat">→</i></div></div><a href={c.source} target="_blank" rel="noreferrer">Read the company&#39;s original report ↗</a></article>
  <article className="card health-card"><SectionHead eyebrow="QUALITY SCORECARD" title="Five dimensions, not one ratio"/><div className="health-bars">{c.health.map((m,i)=><Bar key={m.label} label={m.label} value={m.value} tone={i===3?"gold":"green"}/>)}</div><p><b>Agent read:</b> Business quality is strong; valuation is the weakest relative dimension. Scores summarize the evidence set, not future returns.</p></article><article className="card valuation-card"><SectionHead eyebrow="VALUATION CONTEXT" title="What the market is pricing in"/><div className="valuation-scale"><div><span>15×</span><span>25× S&P 500</span><span>45×</span></div><section><i style={{left:`${Math.min(92,Math.max(6,(parseFloat(pe)-15)/30*100))}%`}}><b>{ticker}<br/>{pe}</b></i><em style={{left:"33%"}}/></section></div><div className="valuation-notes"><p><span>Current question</span><b>Can earnings grow fast enough to justify the premium?</b></p><p><span>Do not use alone</span><b>Pair P/E with margin durability, capital needs and growth.</b></p></div><button onClick={()=>explain("P/E ratio")}>Explain valuation in chat →</button></article>
  <article className="card thesis-card"><SectionHead eyebrow="INVESTMENT CASE" title="Reason, risk and next check"/><div><span className="thesis">WHY IT MAY WORK</span><p>{c.thesis}</p></div><div><span className="risk">WHAT MAY GO WRONG</span><p>{c.risk}</p></div><div><span className="catalyst">WHEN TO CHECK AGAIN</span><p>{c.catalyst}</p></div></article><article className="card company-decision"><SectionHead eyebrow="DECISION PATH" title="How the example weight was chosen"/><div className="decision-path"><span>Business check<b>Strong</b></span><i>→</i><span>Arguments<b>{ticker==="NVDA"?"Mixed":"Positive"}</b></span><i>→</i><span>Risk limit<b>{ticker==="NVDA"?"20% maximum":"Within limit"}</b></span><i>→</i><span>Example weight<b>{weight}%</b></span></div><button onClick={()=>ask(`Show the full reasoning for ${ticker}.`,`Open Why these picks to compare the reasons for and against ${ticker} before reading the example action and risk limits.`,"agents")}>See the full reasoning →</button></article></section></details><p className="data-footnote prominent-footnote">A dated label identifies refreshed prices. Charts, scores and company explanations marked as examples are teaching material, not current recommendations. Missing information is shown as “—”.</p></>;
}

function AgentRoom({amount,rebalanced,explain,ask,liveAnalysis,marketUpdated}:{amount:number;rebalanced:boolean;explain:(t:GlossaryTerm)=>void;ask:(q:string,a:string,t?:Tab)=>void;liveAnalysis:LiveAnalysis|null;marketUpdated:boolean}) {
  const secondTicker=rebalanced?"AMZN":"MSFT";
  const demoReports=[{icon:"M",name:"Market Analyst",tag:"TECHNICAL",score:72,verdict:"Constructive trend",items:["Price trend remains above the medium-term base","Momentum is positive but event risk is rising","Staged entry preferred to a single trade"]},{icon:"F",name:"Fundamentals Analyst",tag:"FINANCIALS",score:86,verdict:"Quality supports exposure",items:["Revenue growth is positive across core holdings","Margins and balance sheets support resilience","Valuation limits position size"]},{icon:"N",name:"News Analyst",tag:"EVENTS",score:76,verdict:"Supportive, catalyst-heavy",items:[`AAPL and ${secondTicker} disclosures are constructive`,"Interest-rate policy keeps growth-stock risk relevant","NVIDIA updates require a defined response"]},{icon:"S",name:"Sentiment Analyst",tag:"SENTIMENT",score:61,verdict:"Optimistic, crowded",items:["AI narrative remains positive","Crowding raises reversal sensitivity","Sentiment is secondary to primary evidence"]}];
  const reports=liveAnalysis?.agents.map((r,i)=>({icon:["M","F","N","S"][i],name:r.name,tag:["TECHNICAL","FINANCIALS","EVENTS","SENTIMENT"][i],score:Math.round(r.score),verdict:r.verdict,items:r.findings}))||demoReports;
  if(marketUpdated&&!liveAnalysis)return <><SectionHead eyebrow="WHY THESE PICKS" title="The latest information is ready, but this example has not been re-evaluated." note="The old sample conclusion is hidden so it cannot be mistaken for a decision based on the newest prices and company updates." action={<a className="source-link" href="https://github.com/TauricResearch/TradingAgents" target="_blank" rel="noreferrer">How the research workflow works ↗</a>}/><section className="connect-banner card"><div><h3>No new buy or sell view is being shown</h3><p>You can still review the dated information under Recent updates and My stocks.</p></div></section></>;
  return <><SectionHead eyebrow="WHY THESE PICKS" title="See the reasons for and against the example plan." note={liveAnalysis?"The first cards use the latest generated explanation. Teaching examples below are labeled separately.":"This teaching example shows how different views are compared before a portfolio decision."} action={<a className="source-link" href="https://github.com/TauricResearch/TradingAgents" target="_blank" rel="noreferrer">How the research workflow works ↗</a>}/><section className="agent-pipeline card"><div className="pipeline-group"><small>RESEARCH VIEWS</small><div>{["Market","Business","News","Crowd mood"].map((x,i)=><span key={x}><i>{["M","F","N","S"][i]}</i>{x}<b>{liveAnalysis?"READY":"EXAMPLE"}</b></span>)}</div></div><i>→</i><div className="pipeline-node"><small>COMPARE</small><span><i>↕</i>For / against<b>{liveAnalysis?"READY":"EXAMPLE"}</b></span></div><i>→</i><div className="pipeline-node"><small>PLAN</small><span><i>T</i>Possible action<b>{liveAnalysis?"READY":"EXAMPLE"}</b></span></div><i>→</i><div className="pipeline-node"><small>CHECK</small><span><i>R</i>Risk limits<b>{liveAnalysis?"READY":"EXAMPLE"}</b></span></div><i>→</i><div className="pipeline-node final"><small>RESULT</small><span><i>PM</i>Final view<b>{liveAnalysis?"READY":"EXAMPLE"}</b></span></div></section>
  <section className="agent-reports">{reports.map(r=><article className="card" key={r.name}><header><span>{r.icon}</span><div><small>{liveAnalysis?r.tag:`SAMPLE · ${r.tag}`}</small><h3>{r.name}</h3></div><em>{r.score}/100</em></header><b className="agent-verdict">{r.verdict}</b><ul>{r.items.map(x=><li key={x}>{x}</li>)}</ul><footer><button onClick={()=>ask(`Explain the ${r.name} report.`,`${r.name}: ${r.items.join("; ")}. Its ${r.score}/100 score reflects evidence strength, not certainty.`)}>Ask this agent →</button><span>{r.score>=75?"High":"Medium"} evidence</span></footer></article>)}</section>{liveAnalysis&&<section className="event-mode-note"><b>Current output ends with the analyst cards above.</b><span>The debate, trade ticket and committee panels below remain illustrative workflow examples.</span></section>}
  <section className="debate-grid"><article className="card debate-card bull"><SectionHead eyebrow="BULL RESEARCHER" title="Best case for the portfolio" action={<b>74 / 100</b>}/><ol><li><span>01</span><div><b>Earnings quality</b><p>Recent company disclosures show broad revenue growth and strong operating economics.</p><small>SUPPORTED BY 3 PRIMARY SOURCES</small></div></li><li><span>02</span><div><b>AI and cloud demand</b><p>Contracted cloud demand and semiconductor growth support a multi-year thesis.</p><small>SUPPORTED · HIGH RELEVANCE</small></div></li><li><span>03</span><div><b>Cash provides optionality</b><p>A 15% reserve can fund staged additions during volatility.</p><small>PORTFOLIO CONSTRAINT</small></div></li></ol></article><article className="card debate-center"><p className="eyebrow">RESEARCH MANAGER</p><div className="debate-score"><span>BULL 74</span><i><em style={{width:"56%"}}/></i><span>BEAR 68</span></div><h3>Constructive fundamentals win—<br/>but only with position limits.</h3><p>The Manager retains the Bull growth thesis while accepting the Bear argument on concentration, valuation and event timing.</p><dl><div><dt>Accepted bull claim</dt><dd>Operating momentum</dd></div><div><dt>Accepted bear claim</dt><dd>Valuation fragility</dd></div><div><dt>Unresolved</dt><dd>Next NVIDIA update</dd></div></dl><button onClick={()=>explain("Model confidence")}>How confidence is formed →</button></article><article className="card debate-card bear"><SectionHead eyebrow="BEAR RESEARCHER" title="Strongest challenge" action={<b>68 / 100</b>}/><ol><li><span>01</span><div><b>Factor concentration</b><p>Three companies share mega-cap growth, AI and duration-sensitive drivers.</p><small>CONFIRMED IN RISK MODEL</small></div></li><li><span>02</span><div><b>Valuation asymmetry</b><p>Premium multiples leave less room for earnings or guidance disappointment.</p><small>SUPPORTED BY MARKET SNAPSHOT</small></div></li><li><span>03</span><div><b>Catalyst clustering</b><p>A major NVIDIA update can transmit quickly across correlated AI holdings.</p><small>NEXT COMPANY UPDATE · HIGH IMPACT</small></div></li></ol></article></section>
  <section className="execution-grid"><article className="card trader-ticket"><SectionHead eyebrow="TRADER PROPOSAL" title="Translate research into an action"/><div className="ticket"><span>ACTION<b>BUILD / HOLD</b></span><span>SIZE<b>{wholeMoney(amount)}</b></span><span>ENTRY<b>3 tranches</b></span><span>HORIZON<b>3–5 years</b></span></div><div className="orders"><p><i>AAPL</i><b>35%</b><span>Core now · review margin quality</span></p><p><i>{secondTicker}</i><b>30%</b><span>Build in two entries</span></p><p><i>NVDA</i><b>20%</b><span>Wait for the next material update before adding</span></p><p><i>CASH</i><b>15%</b><span>Hold as optionality</span></p></div><button onClick={()=>ask("Why three entry tranches?","Staging separates the long-term thesis from short-term timing. It reduces the chance that one entry date determines the full cost basis, but it does not guarantee a better price.")}>Challenge execution →</button></article><article className="card risk-committee"><SectionHead eyebrow="RISK COMMITTEE" title="Three lenses test the same trade"/><div className="risk-votes"><div><span className="aggressive">A</span><p><b>Aggressive</b><small>APPROVE</small></p><em>Increase NVIDIA after a strong company update.</em></div><div><span className="neutral">N</span><p><b>Neutral</b><small>APPROVE WITH LIMITS</small></p><em>Keep the 20% NVIDIA cap and 15% cash.</em></div><div><span className="conservative">C</span><p><b>Conservative</b><small>REDUCE</small></p><em>Add a broad-market ETF; lower single-stock risk.</em></div></div><footer><b>Committee synthesis</b><p>The neutral plan best matches the user profile. Aggressive upside is documented; conservative diversification remains a valid alternative.</p></footer></article><article className="card manager-decision"><span>PM</span><p className="eyebrow pale">PORTFOLIO MANAGER</p><h2>APPROVE<br/><em>WITH LIMITS</em></h2><p>Accept the Trader proposal, preserve four hard guardrails, and reopen the decision after the next material evidence event.</p><dl><div><dt>Decision</dt><dd>Educational build / hold</dd></div><div><dt>Confidence</dt><dd>82 / 100</dd></div><div><dt>Next review</dt><dd>After a material company update</dd></div></dl><button onClick={()=>ask("What would change the final decision?","A material earnings miss, margin deterioration, higher risk correlation, a breach of the 18% drawdown alert, or a user constraint change would reopen the workflow.")}>What changes this? →</button></article></section></>;
}

function EventsView({ask,liveMarket}:{ask:(q:string,a:string,t?:Tab)=>void;liveMarket:LiveMarket|null}) {
  const latestNewsTime=liveMarket?.news[0]?.publishedAt;
  const isFiled=liveMarket?.newsTimeBasis==="filed";
  const isMixed=liveMarket?.newsTimeBasis==="mixed";
  const eventTimeDescription=isMixed?"original publication or filing date":isFiled?"official filing date":liveMarket?.newsTimeBasis==="updated"?"publisher's update date":"publisher's publication date";
  return <>
    <SectionHead eyebrow="RECENT UPDATES" title={liveMarket?"What has happened around these companies":"Example company updates"} note={liveMarket?`Each card shows its ${eventTimeDescription}. We last checked for updates ${newsDate(liveMarket.freshness.newsFetchedAt)}.`:"These examples show how a dated company update would appear. Refresh to check for current items."} action={<div className="event-summary"><span><i className={liveMarket?.dataStatus==="stale"?"medium":liveMarket?"high":"medium"}/>{liveMarket?liveMarket.dataStatus==="stale"?"Last available":"Official sources":"Examples"}</span><span><i className="medium"/>{liveMarket?liveMarket.news.length:events.length} matched</span></div>}/>
    {!liveMarket&&<section className="event-mode-note"><b>These are teaching examples, not current updates.</b><span>Select “Refresh prices &amp; news” to check what is available now.</span></section>}
    {liveMarket&&liveMarket.news.length===0&&<section className="event-empty card"><h3>No recent matching articles</h3><p>{liveMarket.sources.news} returned no title-level matches for this portfolio. Older demo events will not be substituted.</p></section>}
    <section className="event-layout"><div className="event-list">{liveMarket?liveMarket.news.map((item,i)=>{const date=newsDateParts(item.publishedAt);const itemBasis=item.timeBasis||liveMarket.newsTimeBasis;const itemTimeLabel=newsBasisLabel(itemBasis);return <article className="card event-row live-event-row" key={item.url||`${item.title}-${i}`}><div className="event-date"><b>{date.day}</b><span>{date.month}</span><i/></div><div className="event-main"><header><span className="event-type mixed">{itemTimeLabel.toUpperCase()}</span><em>{item.symbols.join(" · ")}</em></header><h3>{item.title}</h3><p>{item.summary}</p><footer><a href={item.url} target="_blank" rel="noreferrer">Read the original at {item.source} ↗</a><span>{itemTimeLabel} {newsDate(item.publishedAt)}</span><button onClick={()=>ask(`Discuss this event: ${item.title}`,`${itemTimeLabel} ${newsDate(item.publishedAt)} via ${item.source}. Available context: ${item.summary}`)}>Explain this update →</button></footer></div></article>;}):events.map((e,i)=><article className="card event-row" key={e.title}><div className="event-date"><b>{e.date.split(" ")[0]}</b><span>{e.date.split(" ")[1]}</span><i/></div><div className="event-main"><header><span className={`event-type ${e.tone}`}>EXAMPLE · {e.type}</span><em>{e.ticker}</em><small>{e.severity} impact</small></header><h3>{e.title}</h3><p>{e.summary}</p><div className="event-analysis"><div><span>WHY IT MATTERS</span><p>{e.impact}</p></div><div><span>WHAT TO WATCH</span><p>{e.response}</p></div></div><footer><a href={e.href} target="_blank" rel="noreferrer">Read original source ↗</a><button onClick={()=>ask(`Discuss sample event ${i+1}: ${e.title}`,`${e.impact} Proposed response: ${e.response}`)}>Explain this example →</button></footer></div></article>)}</div><aside><article className="card event-map"><p className="eyebrow">WHY EVENTS MATTER</p><h3>How one update can affect your money</h3><div><span>Company or economy news</span><i>↓</i><span>Expected growth and profit</span><i>↓</i><span>Share price</span><i>↓</i><span>Your portfolio value</span></div></article>{liveMarket?<article className="card event-freshness"><p className="eyebrow">ABOUT THESE DATES</p><h3>{liveMarket.dataStatus==="stale"?"Showing the last available information":"Updates were checked today"}</h3><dl><div><dt>Companies followed</dt><dd>{liveMarket.symbols.join(" · ")}</dd></div><div><dt>We checked for updates</dt><dd>{newsDate(liveMarket.freshness.newsFetchedAt)}</dd></div><div><dt>Newest item we found</dt><dd>{latestNewsTime?newsDate(latestNewsTime):"No recent match"}</dd></div><div><dt>Original publishers</dt><dd>{liveMarket.sources.news}</dd></div></dl><p>{latestNewsTime?"If the newest item is older than today, it means we did not find a newer verified update for these companies.":"We did not find a verified update for these companies. Older examples are not being substituted."}</p></article>:<article className="card catalyst-calendar"><p className="eyebrow">EXAMPLE CALENDAR</p><h3>Events an investor may watch</h3><div><b>26</b><p><span>AUG</span>Company earnings<em>HIGH</em></p></div><div><b>04</b><p><span>SEP</span>US employment data<em>MACRO</em></p></div><div><b>16</b><p><span>SEP</span>Interest-rate decision<em>HIGH</em></p></div></article>}<article className="card source-quality"><p className="eyebrow">WHICH SOURCES MATTER MOST?</p><h3>Start with the original source</h3><Bar label="Company or regulator" value={92}/><Bar label="Established financial news" value={76} tone="gold"/><Bar label="Private research" value={68} tone="purple"/><Bar label="Social media" value={38} tone="gray"/><p>Social posts can raise a question, but an original company or regulator document carries more weight.</p></article></aside></section>
  </>;
}

function RiskView({amount,lossPreference,rebalanced,explain,ask}:{amount:number;lossPreference:string;rebalanced:boolean;explain:(t:GlossaryTerm)=>void;ask:(q:string,a:string,t?:Tab)=>void}) {
  const scenarios=[{name:"Higher rates for longer",p:-9.8,b:-6.2},{name:"AI capex slowdown",p:-12.6,b:-5.3},{name:"Broad risk-off shock",p:-16.1,b:-13.4},{name:"Growth upside",p:14.4,b:9.1}];
  const secondTicker=rebalanced?"AMZN":"MSFT";
  return <><SectionHead eyebrow="RISK CHECK" title="Could you stay calm if this plan fell in value?" note={`You told us your comfort level is “${lossPreference}”. Compare that answer with the dollar examples below.`} action={<button className="outline-action" onClick={()=>ask("Explain the main portfolio risk.","The main concern is that all three stocks depend heavily on large-company growth. They may fall together even though they are different companies.")}>Explain the biggest risk</button>}/><section className="risk-simple"><article className="card"><span className="risk-number">1</span><div><small>BIGGEST RISK</small><h2>Too much depends on large technology and growth companies</h2><p>Apple, {rebalanced?"Amazon":"Microsoft"} and NVIDIA are different businesses, but higher interest rates or weaker growth expectations could hurt them at the same time.</p></div></article><article className="card money-risk"><small>EXAMPLE BIGGEST PAST FALL</small><b>−{wholeMoney(amount*.142)}</b><span>−14.2% of a {wholeMoney(amount)} example</span><p>This does not predict the next fall. It shows the size of loss an investor should be emotionally and financially ready to see.</p></article><article className="card money-risk cash"><small>CASH CUSHION</small><b>{wholeMoney(amount*.15)}</b><span>15% of the example stays in cash</span><p>Cash can soften some movement and gives room to invest later, but it cannot prevent losses in the stocks.</p></article></section><section className="risk-actions card"><div><b>Ask yourself one practical question</b><p>If the value temporarily fell from {wholeMoney(amount)} to about {wholeMoney(amount*(1-.142))}, would you sell in panic, hold and review, or add gradually?</p></div><button onClick={()=>ask("What should I do if the portfolio falls 14%?","First check whether your time horizon, emergency savings and reason for owning each stock are unchanged. A past 14% fall does not set a limit on future losses, so use your personal loss capacity rather than the chart alone.")}>Help me think it through →</button></section><details className="advanced-details risk-details"><summary><span><b>See the detailed risk tests</b><small>Market sensitivity, example stress scenarios, shared movement and portfolio limits.</small></span><strong>Show details</strong></summary><section className="risk-kpis"><article><span>OVERALL RISK</span><b>Moderate–High</b><small>More concentrated than the broad market</small></article><article><Learn term="Beta" explain={explain}/><b>1.16</b><small>Historically moved about 16% more than the market</small></article><article><Learn term="Max drawdown" explain={explain}/><b>−14.2%</b><small>Example biggest fall</small></article><article><span>IN THREE STOCKS</span><b>85%</b><small>High concentration</small></article><article><Learn term="Tracking error" explain={explain}/><b>8.4%</b><small>Moved differently from the market</small></article></section>
  <section className="risk-grid"><article className="card stress-card"><SectionHead eyebrow="WHAT-IF SCENARIOS" title="Example portfolio vs S&P 500" note="Illustrative one-period change" action={<div className="legend"><span><i className="green"/>Example</span><span><i className="gray"/>S&P 500</span></div>}/><div className="stress-chart">{scenarios.map(s=><div key={s.name}><span>{s.name}</span><section><i className={s.p>0?"gain":"loss"} style={{width:`${Math.abs(s.p)*4}%`}}/><em className={s.b>0?"gain":"loss"} style={{width:`${Math.abs(s.b)*4}%`}}/></section><b className={s.p>0?"positive":"negative"}>{s.p>0?"+":""}{s.p}%</b></div>)}</div></article><article className="card correlation-card"><SectionHead eyebrow="SHARED MOVEMENT" title="Different names can still fall together" note="Illustrative 1-year comparison"/><div className="matrix"><span/><b>AAPL</b><b>{secondTicker}</b><b>NVDA</b><b>SPY</b>{[["AAPL","1.00",".72",".61",".77"],[secondTicker,".72","1.00",".66",".81"],["NVDA",".61",".66","1.00",".70"],["SPY",".77",".81",".70","1.00"]].map(row=><div className="matrix-row" key={row[0]}><b>{row[0]}</b>{row.slice(1).map((v,i)=><i key={i} style={{opacity:Number(v)}}>{v}</i>)}</div>)}</div><p>These are example values. High shared movement means owning three companies may provide less protection than it first appears.</p></article><article className="card drawdown-card"><SectionHead eyebrow="PAST FALL" title="From a high point, down to the low, then recovery"/><svg viewBox="0 0 600 230" className="drawdown-svg"><line x1="20" y1="58" x2="580" y2="58"/><path d="M20 58 C70 45 105 52 145 61 C190 77 188 169 260 182 C328 194 358 128 403 112 C455 94 505 61 580 50"/><circle cx="260" cy="182" r="5"/><line x1="260" y1="58" x2="260" y2="182" className="measure"/><text x="275" y="119">−14.2% example fall</text><text x="20" y="44">High</text><text x="515" y="38">Recovery</text></svg><button onClick={()=>explain("Max drawdown")}>What does this mean? →</button></article><article className="card guardrail-card"><SectionHead eyebrow="PORTFOLIO LIMITS" title="Rules the explanation cannot ignore"/><Bar label="Maximum in one stock" value={35} max={50} suffix="% / 50%"/><Bar label="Cash kept aside" value={15} max={20} suffix="% / 10%" tone="gold"/><Bar label="Example fall alert" value={14.2} max={18} suffix="% / 18%" tone="purple"/><Bar label="Growth-stock exposure" value={65} max={70} suffix="% / 70%"/><div className="pass"><span>✓</span><p><b>The four example rules are currently within their limits.</b> This does not mean losses are limited or unlikely.</p></div></article><article className="card triggers-card"><SectionHead eyebrow="CHECK AGAIN WHEN" title="What should cause a new review?"/><ul><li><span>01</span><p><b>Your needs change</b>Your budget, time horizon, exclusions or preferred stocks change.</p></li><li><span>02</span><p><b>The company changes</b>Earnings, profit margins, guidance or regulation materially changes.</p></li><li><span>03</span><p><b>A portfolio limit is crossed</b>One position or a possible loss becomes too large.</p></li><li><span>04</span><p><b>The original reason no longer holds</b>The business is no longer doing what justified owning it.</p></li></ul></article></section></details><p className="data-footnote prominent-footnote">All losses and scenarios on this page are teaching examples, not forecasts or maximum-loss guarantees.</p></>;
}

function EvidenceView({rebalanced,explain,ask,openSettings}:{rebalanced:boolean;explain:(t:GlossaryTerm)=>void;ask:(q:string,a:string,t?:Tab)=>void;openSettings:()=>void}) {
  const claims=[{id:"C-01",claim:"Apple quarterly revenue grew 16%",source:"Apple Q3 release",quality:"Primary",used:"Business case",status:"Verified"},rebalanced?{id:"C-02",claim:"Amazon replaced Microsoft in the teaching example",source:"User preference",quality:"User input",used:"Example allocation",status:"Verified"}:{id:"C-02",claim:"Microsoft Cloud revenue grew 27%",source:"Microsoft FY26 Q4",quality:"Primary",used:"Business case",status:"Verified"},{id:"C-03",claim:"NVIDIA Q1 revenue reached $81.6B",source:"NVIDIA / SEC 8-K",quality:"Primary",used:"Example risk limit",status:"Verified"},{id:"C-04",claim:"S&P 500 YTD return was 12.6%",source:"AP market summary",quality:"Press",used:"Example comparison",status:"Corroborated"},{id:"C-05",claim:"Quality-growth basket remains preferred",source:"Private demo report",quality:"Private",used:"Teaching context",status:"Demo"}];
  return <><SectionHead eyebrow="SOURCES & ASSUMPTIONS" title="See what is factual, what is calculated and what is only an example." note="Open original sources, inspect assumptions and understand how the teaching workflow reaches a result." action={<div className="source-actions"><button className="outline-action" onClick={()=>ask("Show unsupported claims.","The example flags selected portfolio statistics as illustrative rather than presenting them as current facts.")}>Find assumptions</button><button className="outline-action" onClick={openSettings}>Project connections</button></div>}/><section className="evidence-grid"><article className="card trace"><SectionHead eyebrow="HOW IT WORKS" title="From your answers to an explained example"/><div>{[{n:"01",t:"Your answers",p:"Goal · amount · time · comfort with loss · preferences"},{n:"02",t:"Dated information",p:"Closing prices · company updates · original sources"},{n:"03",t:"Arguments compared",p:"Reasons it may work · reasons it may not"},{n:"04",t:"Example mix",p:"Stocks · percentages · cash cushion"},{n:"05",t:"Downside check",p:"Concentration · possible falls · review rules"},{n:"06",t:"Plain-language result",p:"What it means · what to watch · what is only an example"}].map((x,i)=><section key={x.n}><span>{x.n}</span><div><b>{x.t}</b><p>{x.p}</p></div>{i<5&&<i>↓</i>}</section>)}</div></article><article className="card confidence-card"><SectionHead eyebrow="EXAMPLE INFORMATION STRENGTH" title="What an 82 / 100 process score means"/><div className="confidence-ring"><span><b>82</b><small>/ 100</small></span></div><Bar label="Fits the stated preferences" value={94}/><Bar label="Original-source coverage" value={88}/><Bar label="Different views agree" value={76} tone="gold"/><Bar label="Information is dated" value={84} tone="purple"/><Bar label="Assumption penalty" value={18} tone="red"/><p><Learn term="Model confidence" explain={explain}/> describes the completeness of an example process. It is not the probability of making money.</p></article><section className="claim-register"><SectionHead eyebrow="CLAIM LIST" title="Where each important statement came from" note="Teaching claims are kept separate from current prices and official updates."/><div className="claim-table"><header><span>CLAIM</span><span>SOURCE</span><span>WHY IT IS USED</span><span>STATUS</span></header>{claims.map(c=><article key={c.id}><div><b>{c.id}</b><p>{c.claim}</p></div><div><span>{c.quality}</span><p>{c.source}</p></div><p>{c.used}</p><em className={c.status.toLowerCase()}>{c.status}</em></article>)}</div></section><article className="card coverage-map"><SectionHead eyebrow="EXAMPLE COVERAGE" title="Where the teaching case has more or less support"/><div className="coverage">{[["Company information",92],["Market comparison",86],["Economy",79],["Private research",58],["Crowd mood",44]].map(x=><div key={x[0]}><span>{x[0]}</span><i><em style={{width:`${x[1]}%`}}/></i><b>{x[1]}%</b></div>)}</div><p>Less support should lower confidence. It should never be filled with invented facts.</p></article><article className="card audit"><SectionHead eyebrow="EXAMPLE CHANGE HISTORY" title="What the page changed"/><div><span>1</span><i className="input">YOU</i><p>Your example preferences were loaded.</p></div><div><span>2</span><i className="data">DATES</i><p>Prices and company updates were labeled with their dates.</p></div><div><span>3</span><i className="agent">EXAMPLE</i><p>The sample allocation and its assumptions were displayed.</p></div><div><span>4</span><i className="risk">RISK</i><p>Possible losses were translated into dollar amounts.</p></div>{rebalanced&&<div><span>5</span><i className="user">CHANGE</i><p>The example changed from MSFT to AMZN at your request; any old generated conclusion was cleared.</p></div>}</article><article className="card limitations"><SectionHead eyebrow="LIMITS" title="What this page does not claim"/><ul><li>Delayed closing prices are not real-time quotes.</li><li>Historical and scenario figures do not forecast future returns.</li><li>Scores and ratios labeled as examples are teaching calculations.</li><li>Private notes are never presented as public facts.</li><li>The example does not place trades or guarantee a maximum loss.</li></ul><button onClick={()=>ask("How should I verify this example?","Open the original company sources, check the date on every price or update, separate current facts from teaching calculations, compare with a broad-market alternative and seek licensed advice when suitability matters.")}>How to verify it responsibly →</button></article></section></>;
}
